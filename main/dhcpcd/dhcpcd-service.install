post_install() {
	/usr/bin/rc-update add dhcpcd default
}

post_upgrade() {
	if /usr/bin/rc-status | grep -q "dhcpcd"; then
		echo "==> dhcpcd needs to be restarted"
	fi
}

post_remove() {
	for _service in $(find /etc/openrc/runlevels/*/ -name 'dhcpcd'); do
		rm ${_service}
	done
}
