post_install() {
	rc-update add consolekit default
}

post_upgrade() {
	if rc-status | grep -q "consolekit"; then
		echo "==> consolekit needs to be restarted"
	fi
}

post_remove() {
	for _service in $(find /etc/openrc/runlevels/*/ -name 'consolekit'); do
		rm ${_service}
	done
}
