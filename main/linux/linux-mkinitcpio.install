post_upgrade() {
	rm -f boot/initramfs-${2}-cromnix.img
	rm -f boot/initramfs-${2}-cromnix-fallback.img
}

post_remove() {
	rm -f boot/initramfs-${1}-cromnix.img
	rm -f boot/initramfs-${1}-cromnix-fallback.img
}
