post_install() {
	/usr/bin/rc-update add alsasound default
}

post_upgrade() {
	if /usr/bin/rc-status | grep -q "alsasound"; then
		echo "==> alsasound needs to be restarted"
	fi
}

post_remove() {
	for _service in $(find /etc/openrc/runlevels/*/ -name 'alsasound'); do
		rm ${_service}
	done
}
