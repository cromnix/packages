post_install() {
	echo "runit-cromnix is capable of running a system on its own. However, because"
	echo "there are no runit runscripts for packages yet, you'll need to write"
	echo "your own runscript for the time being."
}
